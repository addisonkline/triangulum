[project]
name = "triangulum"
version = "2.0.0"
description = "A climate normals estimator for the continental United States"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2.3",
    "scipy>=1.15.2",
    "typer>=0.15.2",
]
